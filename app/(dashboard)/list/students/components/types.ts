export interface Student {
  id: string;
  name: string;
  email: string;
  photo: string;
  phone: string;
  grade: string;
  class: string;
  address: string;
}

export const INITIAL_PAGE = 1;
export const DEFAULT_PAGE_SIZE = 10;
